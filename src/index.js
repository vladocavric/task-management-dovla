const app = require('./app')
const port = process.env.PORT;
//======================================================================================================================
//  SERVER
app.listen(port, (err) => {
    if (err) {
        console.log(err)
    } else {
        console.log(`Server is working on port ${port}`)
    }
});

//======================================================================================================================